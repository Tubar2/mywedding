# Copie este arquivo para .env e preencha com os valores do seu projeto Supabase
# (Project Settings > API). Esses valores NÃO são secretos — o anon key é feito
# para ser público, a segurança de verdade está nas policies de RLS no banco.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
